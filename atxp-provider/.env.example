# ATXP Provider Shim Configuration

# ATXP connection string from CLI (npx atxp create)
# Get this by running: npx atxp create my-project --template agent
ATXP_CONNECTION=https://accounts.atxp.ai?connection_token=your_token_here

# Optional: Provider authentication token
# If set, Hub must include this in Authorization: Bearer header
ATXP_PROVIDER_TOKEN=optional_secret_token

# Server port
PORT=4005
