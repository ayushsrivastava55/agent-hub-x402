# === REQUIRED ===
# Generate with: openssl rand -hex 32
HUB_API_SECRET=your-secret-key-here

# x402 facilitator URL - Choose one:
# Option 1 (Recommended): PayAI - Free, Solana-first, covers network fees
FACILITATOR_URL=https://facilitator.payai.network
# Option 2: x402.rs - Open-source, zero fees, multi-chain
# FACILITATOR_URL=https://facilitator.x402.rs
# Option 3: Coinbase CDP facilitator (requires API keys, see below)
# FACILITATOR_URL=<use @coinbase/x402 facilitator object>

# === OPTIONAL: Redis (for scaling) ===
# Local: redis://localhost:6379/0
# Cloud: Get from https://redis.com/try-free/
REDIS_URL=redis://localhost:6379/0

# === PROTOCOL: ACP (Stripe) ===
# Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_key_here
# Get from https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# === PROTOCOL: ATXP (Circuit/Chisel) ===
# Run: npx atxp create my-project --template agent
# Then copy the connection string into this variable
ATXP_CONNECTION=https://accounts.atxp.ai?connection_token=your_token_here
# Optional: custom provider shim (for internal testing only)
# ATXP_PROVIDER_URL=https://localhost:4005

# === PROTOCOL: AP2 (Google) ===
# Enable strict mandate validation
AP2_STRICT_MANDATE=true
# JWKS URL for JWT verification (provided by Google)
AP2_MANDATE_JWKS=https://www.googleapis.com/oauth2/v3/certs
# Expected issuer for mandate JWTs
AP2_MANDATE_ISSUER=https://accounts.google.com

# === OPTIONAL: Configuration ===
# Idempotency cache TTL (default: 600000 = 10 minutes)
IDEMPOTENCY_TTL_MS=600000
# Rate limit window (default: 60000 = 1 minute)
RATE_LIMIT_WINDOW_MS=60000
# Max requests per IP per window (default: 100)
RATE_LIMIT_MAX=100
# Circuit breaker error threshold % (default: 50)
CB_ERROR_THRESHOLD=50
# x402 timeouts in milliseconds (default: 30000 = 30 seconds)
X402_TIMEOUT_VERIFY_MS=30000
X402_TIMEOUT_SETTLE_MS=30000
X402_TIMEOUT_SUPPORTED_MS=30000
